schema: planwright.fixture.v1
id: malformed-sam
name: Malformed SAM fixture
source_format: sam
source_path: template.txt
compatibility_level: 0
expected_loss_categories: []
commands:
  - name: reject malformed SAM
    args: ["import", "sam", "${source}", "--out", "${temp}/sam.graph.json", "--loss-report", "${temp}/sam-loss.md"]
    want_exit: 2
    want_stderr_contains:
      - "import sam: parse"
